import { PrivateMessageChoiceData } from './private-message-choice-data';

describe('PrivateMessageChoiceData', () => {
  it('should create an instance', () => {
    expect(new PrivateMessageChoiceData()).toBeTruthy();
  });
});
